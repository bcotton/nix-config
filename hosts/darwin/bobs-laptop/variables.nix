{
  # Host-specific variable overrides for bobs-laptop
  # Only include values that differ from hosts/common/variables.nix
  primaryUser = "bcotton";

  # Enable nix.linux-builder for building Linux packages on macOS
  # linuxBuilderEnable = true;
}
